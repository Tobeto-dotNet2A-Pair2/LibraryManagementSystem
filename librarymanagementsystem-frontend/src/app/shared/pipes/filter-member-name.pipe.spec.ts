import { FilterMemberNamePipe } from './filter-member-name.pipe';

describe('FilterMemberNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMemberNamePipe();
    expect(pipe).toBeTruthy();
  });
});
