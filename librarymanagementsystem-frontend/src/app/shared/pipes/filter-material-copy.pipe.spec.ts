import { FilterMaterialCopyPipe } from './filter-material-copy.pipe';

describe('FilterMaterialCopyPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMaterialCopyPipe();
    expect(pipe).toBeTruthy();
  });
});
